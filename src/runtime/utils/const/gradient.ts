import type { NuanceGradient } from '@nui/types'


/** Fallback gradient used when a component does not provide its own. */
export const DEFAULT_GRADIENT: NuanceGradient = {
	from: 'violet.6',
	to: 'grape.6',
	deg: 120,
	bg: 'slate.8',
}
